import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'esspi.energy.com',
  appName: 'ESSPI - ENERGY BACKUP SYSTEM ',
  webDir: 'build'
};

export default config;
